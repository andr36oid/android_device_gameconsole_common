Use buildroot-2024.11, put resizing_defconfig in configs/
Copy scripts and overlay directories to the root of buildroot.

Run:

``make resizing_defconfig``

``make -j$(nproc)``

Copy the rootfs.cpio.gz from output/images/ and move it to the root of the kernel directory or tweak the defconfig to use your path.
Build the kernel with resizing_defconfig from its source.

